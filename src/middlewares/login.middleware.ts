import { NextFunction, Request, Response } from "express";
import {} from "../controllers/login.controller";
import { Next } from "mysql2/typings/mysql/lib/parsers/typeCast";

export const checkBody = async (req:Request, res:Response, next:Next)=>{
    try{

    }
    catch(err){

    }
}


